// app/lib/definitions.ts
export type SessionPayload = {
  userId: string;
  name: string;
  email: string;
  expiresAt: string;
};

// You might also define other types here, like form states
// export type FormState = { ... }
